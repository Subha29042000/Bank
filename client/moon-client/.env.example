# Public API base URL (no trailing slash). Required for GitHub Pages builds.
# Example after deploying backend: https://your-app.onrender.com
# Local dev: leave unset — the app uses http://localhost:5000 automatically.
VITE_API_URL=
